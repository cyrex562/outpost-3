[package]
name = "outpost-core"
version = "0.1.0"
edition = "2021"
license = "MIT"

[lib]
name = "outpost_core"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
ron = "0.8"

[dev-dependencies]
proptest = "1"
