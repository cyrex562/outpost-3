# Architecture Overview (Phase 3.5 Desktop/WASM Pivot)

This document provides a high-level architecture diagram for the desktop/WASM pivot.

See the Mermaid source file at `docs/diagrams/architecture.mmd`.

To view the diagram on GitHub or a Mermaid-capable viewer, open the `.mmd` file or paste its contents into a Mermaid live editor.
