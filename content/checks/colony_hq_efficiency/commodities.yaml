# Commodities used in the colony_hq efficiency balance check.
- id: water
  name: Water
  category: consumable
  base_value: 5.0

- id: oxygen
  name: Oxygen
  category: consumable
  base_value: 6.0

- id: power
  name: Power
  category: energy
  base_value: 0.0

# Referenced only by colony_hq's `construction_cost`, so it never appears in a
# net-rate row — but the loader validates construction-cost commodity refs, and
# the footprint (issue #272 gap 5) reports the metal bill.
- id: structural_metal
  name: Structural Metal
  category: metal
  base_value: 25.0

# Virtual research flow (issue #310). No weight, no cap, not tradeable — the
# real engine drains it from the colony pool into the system-wide research pool
# each turn (outpost_core/src/lib.rs "Research aggregation"), so a positive net
# here means real tech progress.
- id: research
  name: Research
  category: virtual
  base_value: 0.0

# Colony-local compute flow — population morale's sole input (morale.rs,
# issue #382), fed here by colony_hq's baseline trickle (hq_operate_baseline_comms).
- id: networking_compute
  name: Networking / Compute
  category: virtual
  base_value: 0.0
